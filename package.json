{
  "name": "columbus",
  "version": "1.0.0",
  "description": "AI Engine Optimization Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix worker\"",
    "build": "npm run build --prefix frontend && npm run build --prefix worker",
    "start": "npm run preview --prefix frontend",
    "setup": "npm install --prefix frontend && npm install --prefix worker"
  },
  "keywords": [
    "aeo",
    "ai-optimization",
    "chatgpt",
    "claude",
    "gemini",
    "perplexity",
    "seo",
    "saas"
  ],
  "author": "Columbus Team",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@stripe/stripe-js": "^8.5.2",
    "stripe": "^20.0.0"
  }
}
